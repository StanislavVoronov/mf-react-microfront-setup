// Технический entry для Rsbuild.
//
// Это микросервисное приложение НЕ монтирует себя само: тут нет
// ReactDOM.createRoot и нет index.html. Всё, что уезжает в хост,
// объявлено в `exposes` внутри rsbuild.config.ts.
//
// Файл существует только потому, что сборщику нужна хотя бы одна точка
// входа; в этот чанк Rsbuild кладёт dev-рантайм (HMR-клиент, react-refresh),
// который подтягивается вместе с контейнером remoteEntry.js.
export {};
